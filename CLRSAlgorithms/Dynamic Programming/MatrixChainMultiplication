#include <vector>


/*
    matrixDims: array of size N+1 where matrixDims[i-1],matrixDims[i] are the dimensions of the i-th matrix
    we want to return the indices of the matrices in the most efficient multiplication order
*/
std::vector<int> matrixChainMultiplication(std::vector<int> matrixDims){

}